/*
This is put inside a function to check the deferred prompt.

if(deferredPrompt){
    deferredPrompt.prompt();
    deferredPrompt.userChoice.then((choiceResult)=>{

        if(choiceResult.outcome === 'dismissed'){

        }
    })
    deferredPrompt=null;
}

*/

console.log("Iam feed");

/*

if('caches' in window){ //This will come when user saves it. 'Cache on Demand' - Useful when 
you have a resource that needs to be cached like an article to read later 
    caches.open('user-requested').then()
}



*/

